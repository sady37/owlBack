# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/
dist/
**/.bin/

# Test binary
*.test

# Output of the go coverage tool
*.out

# Dependency directories
vendor/

# Go workspace file
go.work

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Environment variables
.env
.env.local
.env.precut-backup
.env.*-backup

# Logs
*.log
logs/

# Config files with secrets
config/*.yaml
config/*.yml
!config/*.example.yaml
!config/*.example.yml

# Docker
docker-compose.override.yml

# OS
.DS_Store
Thumbs.db

# Per-machine: MQTT persistence + TLS (each env keeps its own; not tracked)
mqtt/data/mosquitto.db
server.crt
server.key
owl-common/server.crt
owl-common/server.key
owl-common/cert/

# KMS secrets (per-machine) — never commit key material, any location
kms/master_key*.json
kms/mw.*
kms/owl-kms
kms-backup/
**/master_key-*.json
**/mw.pgp
**/mw.md
vault/

# MQTT TLS (per-env)
mqtt/config/*.pem
mqtt/config/*.key

# One-shot dev tools (built artifacts)
wisefido-data/sleepace-set-realtime

# Stale binaries
wisefido-data/wisefido-data
wisefido-qinglan/wisefido-qinglan
wisefido-sensor/feedback-once
wisefido-sensor/roomengine-playback
wisefido-sensor/roomengine-api

# Local debug artifacts (not for repo)
doc/playback-*.html
out/playback_*.html
ota/*.zip

# Local debug / scratch artifacts
doc/sleepad-debug/
ota/other/
wisefido-qinglan/cmd/test_parse_main.go

# BIND DDNS runtime journals (regenerated by BIND on first DDNS update)
bind/zones/*.jnl
.kms-secrets

# Stray compiled binaries (use go build into .bin/ not repo root)
wisefido-data/main
wisefido-data/cmd/wisefido-data/wisefido-data
wisefido-sensor/wisefido-sensor

# wisefido-cardaggv1: v2 重写前的本地备份，不入远程（2026-05-17）
wisefido-cardaggv1/
tools/replay/replay

# 本地 replay fixture（含大 window.json/Xsensor.log，仅本地排查用，不入远程）
doc/cases/case-D523-0623-11001132/
doc/cases/case-d523-0623-08550926/
