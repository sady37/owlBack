database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "postgres"
  database: "owl_v2"
  sslmode: "disable"

redis:
  addr: "127.0.0.1:6379"
  password: "TeLunSu-36kr"
  db: 0

logging:
  level: "info"
  format: "text"
  output: "stdout"
  filepath: "/var/log/wisefido-cardagg.log"

streams:
  default:
    max_len: 1000
    retention_seconds: 86400

# AI 派生 track verdict 合并行为（S5a AIOverrideCache）。
# 部署约定（域名区分）：
#   test.wisefido.com  → sandbox（仅 log，不动 track_confidence）
#   owl.wisefido.com   → release（覆写 track_confidence + ai_source）
# env override：CARDAGG_AI_OVERRIDE_MODE
ai:
  override_mode: "sandbox"
  override_ttl_sec: 60

# display 周期重建：事件触发是主路径；本 ticker 是兜底（5min）。
# dev 阶段可下调到 30s 加速验证；生产 300s 即可。
display:
  rebuild_interval_sec: 300
